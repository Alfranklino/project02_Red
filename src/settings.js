//Declare that constant to be used in other files. in that files, we just have to call the import statement.
export const SVG_NS = 'http://www.w3.org/2000/svg';
//Sounds
export const PING_COLLISION = '../public/sounds/pong-01.wav';
export const GOAL_SOUND = '../public/sounds/pong-05-goal.wav';

export const KEYS = {
    a: 'a',        // player 1 up key
    z: 'z',        // player 1 down key
    x: 'x',       // player 1 fire key
    up: 'ArrowUp',       // player 2 up key
    down: 'ArrowDown',     // player 2 down key
    left: 'ArrowLeft',     // player 2 fire key
    spaceBar: ' ' // we'll use this later...
  }